server:
  port: 8888

management:
  endpoints:
    web:
      exposure:
        include: info, health, prometheus
  metrics:
    export:
      atlas:
        enabled: false
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/helloapp?autoReconnect=true
    username: root
    password: baza1
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create
#    properties:
#      hibernate:
#        dialect: org.hibernate.dialect.MySQLDialect