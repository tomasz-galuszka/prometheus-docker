spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:db;DB_CLOSE_DELAY=-1;LOCK_TIMEOUT=10000;
    username: sa
    password: sa
  jpa:
    show-sql: false
    hibernate:
      ddl-auto: create
